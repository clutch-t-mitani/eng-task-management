# 開発タスク分割

## 6. 開発タスク分割

### P0: 環境構築（8タスク）

| ID | タスク |
|---|---|
| P0-1 | Laravel 11 プロジェクト作成 + Laravel Sail 導入 |
| P0-2 | Vite + Vue 3 統合（vite.config.js、app.blade.php） |
| P0-3 | Pinia・Vue Router 4・vuedraggable・Axios インストール |
| P0-4 | Laravel Sanctum インストール・設定（sanctum.php, cors.php） |
| P0-5 | `.env` 設定（DB_*、SESSION_DOMAIN、SANCTUM_STATEFUL_DOMAINS） |
| P0-6 | SPAフォールバックルート定義（routes/web.php） |
| P0-7 | Axios グローバル設定（bootstrap.js） |
| P0-8 | AppLayout・Sidebar・router-view の基本構造（App.vue 移植） |

### P1: 認証機能（10タスク）

| ID | タスク |
|---|---|
| P1-1 | マイグレーション: users（deleted_at 追加） |
| P1-2 | User モデル（SoftDeletes、$fillable、$hidden） |
| P1-3 | DirectorSeeder 作成（初回ディレクター 1 件） |
| P1-4 | AuthController 実装（login / logout / me） |
| P1-5 | 認証 API ルート定義 |
| P1-6 | Pinia authStore 実装（login / logout / fetchMe） |
| P1-7 | LoginView.vue 実装（フォーム・エラー表示・遷移） |
| P1-8 | Vue Router ナビゲーションガード実装 |
| P1-9 | サイドバーにログアウトボタン追加 |
| P1-10 | 認証動作確認（CSRFフロー・リロード状態維持・ログアウト） |

### P2: マスタ管理（12タスク）

| ID | タスク |
|---|---|
| P2-1 | マイグレーション: engineers・products |
| P2-2 | Engineer モデル（SoftDeletes、リレーション）・Product モデル（リレーション） |
| P2-3 | EngineerController（index / store / update / destroy / reorder） |
| P2-4 | ProductController（index / store / update / destroy / reorder。削除時422チェックはP3でissues作成後に有効化） |
| P2-5 | UserController（index / store / show / update / destroy、メールアドレス・パスワード変更対応） |
| P2-6 | マスタ API ルート定義（auth:sanctum ミドルウェア） |
| P2-7 | Pinia userStore・engineerStore・productStore 実装 |
| P2-8 | EngineerListView.vue（一覧・追加・編集・削除・ConfirmDialog） |
| P2-9 | UserListView.vue / UserFormView.vue（追加・編集・削除・ConfirmDialog） |
| P2-10 | ProductListView.vue（一覧・追加・編集・削除・表示順変更・リポジトリ連携設定への導線） |
| P2-11 | サイドバーにユーザー管理・エンジニア管理・プロダクト管理リンク追加 |
| P2-12 | DevelopmentSeeder（エンジニア3名・プロダクト2件・ディレクター2名） |
| P2-13 | マスタ管理 Feature Test（CRUD・reorder・未認証401・バリデーション422） |

### P3: ISSUE管理（11タスク）

| ID | タスク |
|---|---|
| P3-1 | マイグレーション: issues（display_order・github_url・github_issue_number・github_state・github_synced_at・`(product_id, github_issue_number)` UNIQUE含む）・issue_schedules（日付4項目） |
| P3-2 | Issue モデル（リレーション、スコープ） |
| P3-3 | IssueSchedule モデル |
| P3-4 | IssueController（index / show / update / updateStatus / toggleManaged / destroy。group_id 更新はP4で対応。destroy は物理削除せず管理対象から除外。新規作成はGitHub取り込みのみ） |
| P3-5 | スケジュール更新（PUT /issues/{id}/schedule、日付4項目を手動upsert） |
| P3-6 | IssueResource（director・engineer・schedule・GitHub3項目・is_overdue・is_due_soon 含む。group はP4で追加） |
| P3-7 | ISSUE API ルート定義 |
| P3-8 | Pinia issueStore 実装（管理項目更新アクション、楽観的更新はステータス変更のみ） |
| P3-9 | GitHub取り込み済み前提のISSUEシードデータ追加（モックデータを DevelopmentSeeder に移植） |
| P3-10 | ISSUE管理 Feature Test（index/show/update/status/schedule/managed/destroy・複数値/未割当/日付/フラグ絞り込み・未認証401・バリデーション422・GitHub由来項目保護） |
| P3-11 | ISSUE管理項目更新の動作確認 |

### P4: 管理表（14タスク）

| ID | タスク |
|---|---|
| P4-1 | マイグレーション: groups・group_issues・既存groupsのプロダクト非依存化（既存データ保持、全体表示順への正規化、product_id削除） |
| P4-2 | Group・GroupIssue モデル（リレーション） |
| P4-3 | GroupController（index / store / update / destroy / reorder） |
| P4-4 | GroupIssueController（reorder / add / remove） |
| P4-5 | TableController（GET /api/v1/table、管理対象ISSUEのみをグループ+ISSUE+スケジュールで一括取得） |
| P4-6 | グループ・ISSUEシードデータ追加 |
| P4-7 | Pinia groupStore 実装（fetchTable / グループ・グループ内・未グループ reorder / CRUD） |
| P4-8 | Pinia filterStore 実装（asParams getter） |
| P4-9 | FilterBar.vue 改修（filterStore.setFilter() を呼ぶよう変更） |
| P4-10 | GroupSection.vue 改修（APIデータ対応、グループ内D&D、グループ編集・削除） |
| P4-11 | IssueRow.vue 改修（APIデータ対応、GitHubタイトルリンク、プロダクト表示、エンジニア・ステータス・日付インライン編集、期限状態の色表示） |
| P4-12 | IssueResource へ product_name / group 情報追加、Issue 更新で group 変更対応 |
| P4-13 | TableView.vue 実装（グループ間・グループ内・未グループD&D、filterStore watch、一括操作、グループモーダル制御） |
| P4-14 | 管理表 Feature Test + 動作確認（groups CRUD/reorder、グループ内・未グループISSUE並び替え、ISSUE一括移動・管理表除外、GET /api/v1/table、未認証401、バリデーション422、別プロダクト混在、フィルター・D&D・ステータス変更・グループ操作） |

### P5: ダッシュボード（8タスク）

> 現在はP5-1〜P5-7まで実装済み。P5-8はGitHubからの実取り込みが必要なため、P6完了後の統合確認として実施する。推奨順序は `P5-1〜P5-7 → P6-1〜P6-17 → P5-8` とする。

| ID | タスク |
|---|---|
| P5-1 | DashboardAggregator サービス（管理対象ISSUEの全体集計・エンジニア別集計。未管理除外、未割当はsummaryのみに含め、担当中の削除済みエンジニアはdeleted=trueで返す） |
| P5-2 | DashboardController（GET /api/v1/dashboard） |
| P5-3 | ダッシュボード API ルート定義（auth:sanctum 配下） |
| P5-4 | DashboardView.vue 改修（APIから集計データ取得、モックデータ依存除去、loading/error/empty表示） |
| P5-5 | SummaryBar.vue 実装（総ISSUE・完了・作業中・期日超過・完了率の集計カード帯を独立コンポーネント化） |
| P5-6 | EngineerCard.vue 改修（APIのエンジニア別集計データをpropsで受取。director_id混在集計を廃止しengineer_idベースに統一） |
| P5-7 | ダッシュボード Feature Test + 動作確認（未認証401、管理対象のみ集計、未割当のsummary反映、担当ISSUE数・進捗・期日超過バッジ・完了率） |
| P5-8 | 全体通し確認（認証→GitHub取り込み済みIssueを管理表へ追加→ステータス変更→D&D→ダッシュボード反映） |

### P6: GitHub連携（Webhook自動反映 + 再同期）（17タスク）

| ID | タスク |
|---|---|
| P6-1 | マイグレーション: product_repositories（owner/repoの小文字正規化、is_active、成功/partialのみを保持する最終同期日時・ステータス、`UNIQUE(product_id)`、`UNIQUE(owner, repo)`） |
| P6-2 | GitHub同期で利用するissues既存カラム、`(product_id, github_issue_number)` UNIQUE、issue_schedulesの1対1制約をスキーマテストで固定 |
| P6-3 | マイグレーション: sync_logs（件数集計・`running` を含むstatus・error_message・started_at・finished_at・`attempt_count DEFAULT 1`、`UNIQUE(github_delivery_id)`） |
| P6-4 | ProductRepository / SyncLogモデル・Enum・Resource（`trigger` / `github_delivery_id` / `attempt_count` を含む）、IssueモデルのGitHubカラムキャスト確認、Productへのrepository / syncLogsリレーション追加 |
| P6-5 | `config/services.php` にgithub設定（token・webhook_secret・API version・HTTP timeout・同期全体上限120秒/100ページ・再同期ロックTTL=3600秒・Webhook行ロック待機=2秒・表示順採番ロックTTL=30秒/待機=5秒）を追加、`.env.example` に `GITHUB_TOKEN` / `GITHUB_WEBHOOK_SECRET` 追記、Secret未設定時はfail-closedとする |
| P6-6 | GitHubIssueImportService実装（Webhook/API共通のPR除外、未登録openのみ新規作成、未登録closedのskip、登録済みopen/closedの更新、Issue単位transaction、冪等Upsert、`github-import:display-order`ロック（TTL 30秒・最大5秒待機・commitまで保持）付き採番、schedule作成、ツール独自項目保護、一意制約競合の再読み込み、Issue単位失敗時の後続続行と件数返却） |
| P6-7 | GitHubWebhookController実装（`'sha256=' . hash_hmac(...)` とraw bodyの署名検証、必須ヘッダ・JSON・Issue payload検証、`transferred` / PR含む対象外の202 skipとskippedログ、success/skipped deliveryの重複排除、failed deliveryの同一SyncLog再利用・ロック取得後のstatus再判定・Issue反映とログ確定commitまでの行ロック保持・`attempt_count`加算・再試行、同時競合時のstatus再判定、未登録/無効リポジトリskip、成功transactionと失敗ログtransactionの分離） |
| P6-8 | GitHubSyncService実装（`running` ログ作成、`state=all` で未登録openの取り込み・未登録closedのskip・登録済みIssueの状態更新、database cacheの `github-sync:product:{id}` 非待機排他・TTL 3600秒・所有者によるfinally解放、最大100ページ/全体120秒の上限付きページング・timeout、接続例外/500/502/503/504のみ初回+最大2回retry、PR除外、レート制限・401/403/404/422/429/5xx・不正JSON、Issue単位失敗後の続行、反映1件以上の中断=partial・0件=failed、秘密情報を除いたSyncLog確定、success/partial時のみ最終反映状態を更新） |
| P6-9 | UpdateProductRepositoryRequest + 連携設定Service（owner/repo形式検証・小文字正規化、別プロダクトとの重複拒否、Issue取り込み後の別リポジトリ変更拒否、同一リポジトリ再有効化） |
| P6-10 | ProductRepositoryController（show / update / destroy=無効化）+ SyncController（store / index、limit=1〜100バリデーション、`started_at DESC, id DESC`）+ `trigger` / `github_delivery_id` を含むAPI Resource / Resource Collection |
| P6-11 | ルート定義（署名認証の `/github/webhook` はSanctum外、repository / sync / sync-logsは `auth:sanctum` 内）と未認証・HTTPステータスのFeature Test |
| P6-12 | 実装済み `IssueController@index?unmanaged_imports=true` の回帰テストを追加し、UnmanagedImportsPanelとのレスポンス契約を固定 |
| P6-13 | Pinia githubSyncStore実装（fetch/save/deactivate repository、resyncNow、fetchLogs、404/409/422の状態管理、同期後のIssue/管理表再取得） |
| P6-14 | ProductRepositoryView.vue実装（owner/repo、Webhook URL・Secret設定案内、有効/解除状態、最終反映状態、ログ、リポジトリ変更制限の説明） |
| P6-15 | 画面実装：GitHubResyncButton.vue / UnmanagedImportsPanel.vue / IssueRowのgithub_stateバッジ / Sidebar導線。loading/error/empty/disabled、無効化済みリポジトリでの再同期無効化、同期結果トーストを含む |
| P6-16 | GitHub連携自動テストを4グループで実装する。①ImportService：PR除外、未登録open/closed、登録済みclosed/reopened、管理項目保護、display_orderロックの重複採番防止・commitまでの保持・取得タイムアウト。②Webhook：`sha256=`付き署名/Secret未設定、必須ヘッダ・長さ・JSON・payloadの422とログ有無、success/skipped再送、failed再送の成功/再失敗と`attempt_count`、failed同時再送の行ロック・status再判定、行ロック2秒超過時の500、skip理由コード、10秒以内の応答。③リポジトリ設定/ログAPI：登録・正規化・重複・変更制限・無効化/再有効化、Resourceのフィールド・並び順・limit。④Http::fake再同期：ページング、100ページ/120秒上限、排他409・finally解放・TTL失効後の再実行、`running` ログ、無効化済み、retry/非retry、failed/partial判定、Issue単位失敗後の続行と件数、最終反映状態保持、ログ確定 |
| P6-17 | フロントエンドbuild/手動結合確認 + 公開到達可能なWebhook URL、GitHub側と一致するSecret、後片付け可能な検証用実リポジトリを用意したスモークテスト（初回取り込みで未登録openのみ作成・未登録closedはskip、Webhook自動反映、取り込み済みIssueのclosed/reopened、再同期で新規open作成と既存closed状態更新、未追加から管理表追加、failed delivery再送、試行回数・ログ表示）。検証用Issue/Webhook設定を後片付けし、完了後にP5-8の全体通し確認を実施 |
